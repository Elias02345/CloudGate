app_version: v0.5.0
# CloudGate needs no special Linux capabilities for the default packaging
# (container port 8080 only, unprivileged). If the optional local-nginx
# reverse-proxy mode is enabled with ports below 1024, NET_BIND_SERVICE would
# be required the way adguard-home documents it; re-run
# generate_metadata.py before shipping that mode.
capabilities: []
categories:
  - networking
changelog_url: https://github.com/Elias02345/CloudGate/blob/main/CHANGELOG.md
date_added: "2026-09-19"
description: CloudGate puts your homelab services on the internet without port forwarding, using Cloudflare Tunnels and playit.gg tunnels, managed from one web interface.
home: https://github.com/Elias02345/CloudGate
host_mounts: []
# Filled in by the TrueNAS reviewer with the CDN URL once the PR is opened,
# per CONTRIBUTIONS.md ("Icons and screenshots will be hosted on the TrueNAS
# CDN... The PR reviewer will upload them and provide you with the correct
# URLs"). See packaging/README.md.
icon: https://media.sys.truenas.net/apps/cloudgate/icons/icon.png
keywords:
  - networking
  - cloudflare
  - tunnel
  - reverse-proxy
  - cgnat
  - playit
lib_version: 2.3.13
lib_version_hash: "644cb2c53051941e9128fbd4d666094573af5c57a62c037bc56820d25427e454"
maintainers:
  - email: 117339219+Elias02345@users.noreply.github.com
    name: Elias02345
    url: https://github.com/Elias02345
name: cloudgate
run_as_context:
  - description: Container [cloudgate] starts as root. It supervises nginx,
      cloudflared and the playit agent and reloads them when hosts change; the
      Recovery UI runs as an unprivileged user. All capabilities are dropped
      except CHOWN, DAC_OVERRIDE, FOWNER, SETUID, SETGID, NET_BIND_SERVICE and
      KILL.
    gid: 0
    group_name: Host group is [root]
    uid: 0
    user_name: Host user is [root]
# Screenshots exist at packaging/assets/screenshots/ and are linked in the
# PR description; filled in here with their CDN URLs once the PR reviewer
# hosts them. See packaging/README.md.
screenshots: []
sources:
  - https://github.com/Elias02345/CloudGate
title: CloudGate
train: community
version: 1.0.0
